// @conductor/shared — Zod schemas, inferred types, and cross-boundary constants.
// Populated in Unit 02. Placeholder export keeps the package buildable.

export const SHARED_PACKAGE = "@conductor/shared" as const;
