{
  "extends": "../../tsconfig.base.json",
  "comment": "types:[node] — the pg Pool type needs node ambient types.",
  "compilerOptions": {
    "types": ["node"]
  },
  "include": ["src/**/*.ts"],
  "exclude": ["node_modules"]
}
