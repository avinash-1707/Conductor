{
  "name": "@conductor/worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "echo \"worker dev added in Unit 03\" && exit 0",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint .",
    "test": "echo \"no tests yet\" && exit 0"
  },
  "dependencies": {
    "@conductor/shared": "workspace:*"
  }
}
